services:
  - type: web
    name: app-sete-coracoes
    env: node
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: banco-data
      mountPath: /data
      sizeGB: 1